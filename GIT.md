# Github

```cmd

Quick setup — if you’ve done this kind of thing before
or

Configuración rápida: si ha hecho este tipo de cosas antes
o


Get started by creating a new file or uploading an existing file. We recommend every repository include a README, LICENSE, and .gitignore.
…or create a new repository on the command line

Comience por crear un nuevo archivo o cargar un archivo existente. Recomendamos que cada repositorio incluya un readme, licencia y .gitignore.
... o cree un nuevo repositorio en la línea de comandos

echo "# crud-React" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/corbaz/crud-React.git
git push -u origin main

…or push an existing repository from the command line

... o presione un repositorio existente desde la línea de comandos

git remote add origin https://github.com/corbaz/crud-React.git
git branch -M main
git push -u origin main


…or import code from another repository

... o código de importación de otro repositorio

You can initialize this repository with code from a Subversion, Mercurial, or TFS project.

Puede inicializar este repositorio con código de un proyecto de subversión, mercurial o TFS.
```
